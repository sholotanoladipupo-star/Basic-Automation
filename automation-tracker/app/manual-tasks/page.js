import ManualTasksTracker from "@/components/manual-tasks-tracker";

export const metadata = {
  title: "Manual Tasks Register · SRE",
  description: "Card Payment SRE — Manual Work Audit & Automation Coverage",
};

export default function ManualTasksPage() {
  return <ManualTasksTracker />;
}
